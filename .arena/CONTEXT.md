# Shadow Lands — контекст для AI-сессий

> Этот файл читается в начале каждой сессии вместо полного сканирования репозитория.
> Обновляй при появлении новых архитектурных решений, новых модулей и при смене приоритетов.

## Проект

**Shadow Lands** — Telegram MMORPG. Два независимых стека в одном репозитории.

| Стек | Технологии | Entry point | Где работает |
|---|---|---|---|
| A — GitHub Pages | Чистый Python + Pyodide в браузере | `index.html` → `webapp/boot.py` | `alfagideon.github.io` |
| B — Сервер | aiogram + FastAPI + SQLAlchemy | `launch.py` | Локально / Render / VPS |

Правило из `README.md`: новая игровая механика должна попасть в `engine/`, новый раздел панели — в `webapp/pages/` + `webapp/actions/`, и оба — в `modules.json`. `tests/test_wiring.py` проверяет это.

## ⚠️ Правила, обязательные к соблюдению

Читаются первыми, нарушение роняет тесты — не полагайтесь на память.

### 1. Паритет стеков обязателен

**Каждая механика делается сразу для обоих стеков**, а не для одного:

| Стек | Куда писать |
|---|---|
| A (браузер) | `engine/` — правила, `webapp/pages/` + `webapp/actions/` — панель |
| B (сервер) | `core/` — модели и логика, `bot/handlers/` — бот, `admin/` — панель |

Не «сделаю в engine, потом когда-нибудь перенесу»: так уже разъехались
подземелья (жили только на сервере) и защищённый карман (только в браузере),
и разрыв всплыл спустя несколько итераций.

Если паритет **осознанно** отложен (например, нужна миграция БД) — впишите
механику в реестр `tests/test_parity.py` с причиной в поле `todo`. Тест
падает, если механика есть в одном стеке и причина не указана, а также если
новый модуль `engine/` вообще забыли внести в реестр.

Числа, общие для стеков (лимиты, множители), обязаны совпадать — это тоже
проверяется. Новую пару констант добавляйте в `test_shared_numbers_match`.

### 2. Лимит длины модуля — 500 строк

Стережёт `tests/test_wiring.py`. Не дробите логику на огрызки ради цифры:
предел большой намеренно, но при его достижении модуль правда пора делить
по смыслу (например, правила отдельно, экраны отдельно — как
`engine/dungeon.py` и `engine/dungeonui.py`).

### 3. Прочее, что проверяется автоматически

- `engine/` не импортирует `js` и `webapp`;
- `webapp/pages/*` только возвращают HTML-строки, не трогают DOM;
- каждый `data-act` из разметки имеет обработчик в `webapp/actions/`;
- новый модуль обязан попасть в `modules.json` **и** в `FALLBACK`
  внутри `index.html` (списки должны совпадать по составу и порядку);
- перед завершением работы: `python3 tests/run_all.py` — всё зелёное.

## Директории

```
admin/           серверная админ-панель (стек B): FastAPI + Jinja2 + vanilla CSS/JS
bot/             aiogram-бот (стек B)
core/            серверные модели, БД, игровая логика (стек B)
engine/          чистая игровая логика без зависимостей (общая для обоих стеков)
webapp/          клиентская админ-панель в браузере (стек A)
admin-login/     статическая прокладка входа для GitHub Pages
tests/           тесты
modules.json     манифест загрузки модулей в Pyodide
```

## Ключевые файлы

- `admin/main.py` — точка входа серверной админки. ~2900 строк, множество FastAPI-маршрутов.
- `admin/static/style.css` — единый CSS админки.
- `admin/templates/` — шаблоны Jinja2.
- `core/models.py` — SQLAlchemy-модели.
- `core/database.py` — инициализация БД и `async_session`.
- `engine/permissions.py` — ранги и точечные права админов.
- `bot/runner.py` — управление жизненным циклом бота.
- `launch.py` — запуск серверного стека.

## Текущий фокус

- Развитие игрового мира по планам `IDEAS-world.md` (закрыт) и
  `IDEAS-next.md` (в работе).
- **Паритет достигнут: 20 из 20 механик в обоих стеках** (`tests/test_parity.py`).
  Держите его: новая механика сразу в оба, иначе тест упадёт.

## Конвенции

- Роли админов: `viewer`, `moderator`, `admin` + точечные права `CAP_KEYS`.
- Проверка прав в админке: `guard(request, cap)` из `admin/main.py`.
- Все POST-редиректы используют `RedirectResponse(..., status_code=303)`.
- Шаблоны наследуют `base.html`.
- Статика раздаётся с `admin/static/` через `app.mount("/static", ...)`.

## Как работать с областями

| Задача | Смотреть сначала |
|---|---|
| Новый экран админки | `admin/ADMIN_README.md`, `admin/templates/base.html`, `admin/static/style.css` |
| Права доступа | `engine/permissions.py`, `admin/auth.py` |
| Игровая логика | `engine/` + `core/` |
| Бот | `bot/BOT_README.md` (если есть) или `bot/main.py`, `bot/runner.py` |
| База данных | `core/models.py`, `core/database.py`, миграции — `core/migrations.py` |
| Новая механика | `.arena/CONTEXT.md` (правила выше) → `tests/test_parity.py` (реестр) |
| Серверные механики мира | `core/worldevents.py`, `core/factions.py`, `core/death.py`, `core/landmarks.py`, `core/behavior.py` |
| Что делать дальше | `IDEAS-next.md` |

## Заметки между сессиями

См. `.arena/session-notes.md`.
