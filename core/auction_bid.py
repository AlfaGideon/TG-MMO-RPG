"""Аукцион со ставками — ЗАГЛУШКА, механика не реализована.

⚠️ Этот модуль не подключён нигде: ни в `bot/`, ни в `admin/`, ни в
`core/`, ни в тестах. Единственная константа ниже никем не читается
(проверено `grep -rn "auction_bid\\|BID_AUCTION_KEY"` по всему
репозиторию). Файл сохранён как явная точка расширения, а не как
рабочая система — чтобы его случайно не приняли за готовый механизм.

Что есть сейчас: обычный аукцион с фиксированной ценой —
`core/auction.py` + `bot/handlers/auction.py` (покупка по кнопке, лоты
скупщика, возврат непроданного через сутки).

Чего нет: торгов со ставками (несколько игроков поднимают цену, лот
уходит к последнему поднявшему по истечении таймера).

Чтобы реализовать, понадобится:
  1. модель `AuctionBid` (лот, участник, ставка, время) в `core/models.py`
     либо поле `current_bid`/`current_bidder_id` у `AuctionLot`;
  2. атомарное повышение ставки условным UPDATE — иначе два одновременных
     бида затрут друг друга (та же гонка, что чинили в `core/auction.py`,
     см. `AUDIT-BUGS.md`, п. 1);
  3. закрытие торгов в фоновом цикле `bot/runner.py` рядом с
     `sweep_expired`, возврат ставок проигравшим;
  4. экраны в `bot/handlers/auction.py` и колонка в `/editor/auction`.

Альтернатива — удалить файл: если торги со ставками не планируются,
мёртвый модуль лучше не держать. Решение зафиксировано в `IDEAS-100.md`,
пункт № 59.
"""

# Ключ для будущего хранения состояния торгов в AppSetting.
# Пока не читается ни одной строкой кода.
BID_AUCTION_KEY = "bid_auction"
