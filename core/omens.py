"""Знамения: серверная часть.

Каталог знамений — один на оба стека и лежит в `engine/omens.py`
(единственный источник правды, как каталог бедствий для
`core/worldevents.py`). Здесь только реэкспорт и точка вызова для бота.
"""
from engine.omens import OMENS, omen_banner


def get_current_omens() -> list:
    """Текущие знамения. Пока каталог статичен — возвращается весь список."""
    return OMENS
