# 🌑 Shadow Lands — что добавить: интерфейс и механики, которых ещё нет

> Документ — не план сборки, а набор **свежих** предложений, проверенных по
> коду. Он не пересекается с `IDEAS-world.md`, `IDEAS-next.md` и
> `IDEAS-100.md`: ниже только то, чего нет ни в браузерном стеке
> (`engine/` + `webapp/`), ни (в основе) в серверном (`core/` + `bot/`).
> Там, где часть фундамента уже есть, я это пишу прямо в ячейке.

## Как читать

| Метка | Значение |
|---|---|
| 🔎 Нет в коде | `grep` по обоим стекам не нашёл реализацию (команда приведена) |
| 🟡 Есть фундамент | есть смежная система, но самой идеи нет |
| S / M / L | примерно: S — 1 модуль + экран, M — подсистема, L — большая подсистема |
| Первая итерация | минимальный честный кусок, который можно выкатить и проверить руками |

---

# Часть 1. Интерфейс (UX)

## 1.1 🩸 «Пульс героя» — один экран вместо лестницы меню

> ✅ **Реализовано (2026-09-06)** в обоих стеках: `engine/pulse.py` +
> `bot/handlers/pulse.py` + кнопка «🩸 Пульс» в главном меню. Прогон:
> `python3 tests/test_engine_pulse.py`, `tests/test_parity.py` (53/53).

**Сейчас.** Главное меню (`engine/game.py:menu`) — это длинный вертикальный
список из ~18 кнопок. А информации «что вообще происходит с героем»
нет ни в одном месте: HP/MP, активные задания, кулдауны, ближайший босс,
куда ведёт карта сокровищ, сколько осталось до закрытия портала — всё
разбросано по разным командам.

**Идея.** Отдельная кнопка/экран **🧭 Пульс** (или «Сводка»), который
собирает уже существующие данные в одну карточку:

- ❤️/💙 HP/MP, валюта и карма;
- 2–3 активных задания с прогрессом одной строкой;
- 📣 активные события мира: катаклизм, мировой босс, осада, портал — с
  живым таймером;
- ⏳ личные кулдауны (отдых, осада, перекат, духовный торговец),
  если такие есть;
- 📍 текущая цель: «в 5 клетках тайник», «могила в Логове», «NPC в Погосте».

**Что даёт.** Игрок открывает бота, один экран — и понимает, чем заняться
сегодня. Это самое дешёвое улучшение «возвращаемости».

**Куда.** `engine/progress.py` или новый `engine/pulse.py` + кнопка в
`engine/game.py:menu`, таймеры — через уже живущий паттерн `webapp/live_timer.py`.

**Первая итерация.** Без нового состояния: собрать текст из `p.hp`,
`p.quests`, `cataclysm.active()`, `worldboss` и `siege.active`, кнопка в меню.

## 1.2 🧭 Навигатор: компьютерный маршрут и «компас к цели»

**Сейчас.** Есть карта (`engine/mapview.py`) и стрелки на клетке
(`engine/game.py:do_world`), но пути **никак не строятся**: игрок сам
запоминает, как пройти к NPC, тайнику, могиле или боссу. Поиска пути
(`A*`/BFS) нет нигде (`grep -rniE "pathfind|маршрут|компас" engine bot` — пусто).

**Идея.** Построить путь по графу клеток до выбранной цели и показать его:

- на клетке — строку **«➡️ До цели 6 шагов: путь через 🚪/🪜»**;
- на карте — пунктир/стрелку к цели и маркер цели;
- целями могут быть: задание, ваша могила, тайник по карте, NPC, портал,
  ворота осаждаемой цитадели, точка сбора отряда.

**Что даёт.** Убирает главный источник фрустрации в мире с подуровнями и
лестницами: «я знаю, что где-то там, но не знаю, как сюда попасть».

**Куда.** Новый `engine/path.py` (BFS по `Cell.passable` + `floor_links` +
`link`) + подсказка в `texts.cell_view()` / `mapview`.

**Первая итерация.** `path_to(p, target_key)` для одной клетки в том же
мире; кнопка «До цели» в карточке задания и в карточке могилы.

## 1.3 📜 Журнал героя — личная лента вместо разовых сообщений

**Сейчас.** Результат боя, находки, полученные материалы и оплеухи кармы
показываются один раз и исчезают. Отрядные/фракционные строки тоже
одноразовые. «Кто меня убил?», «что упало с того ворга?», «когда сделали
задание?» — вспомнить нельзя.

**Идея.** **Журнал героя** — личная лента последних ~30 событий с фильтрами:

- ⚔️ бои (победа/поражение, добыча, уровень, бестиарий);
- 📦 находки (сундуки, карта сокровищ, археология, собрано);
- 🏠 жизнь (отдых, дом, карман, ломбард);
- 👥 социальное (отряд, гильдия, наставник, дуэль);
- 🌍 мир (знамения, катаклизм, босс, осада).

**Что даёт.** Снимает шум и добавляет «нарратив»: игрок видит историю своего
героя, а не серию исчезающих сообщений.

**Куда.** Поле `p.log: list` (JSON-сериализуемо) или отдельная структура
`p.journal` в `engine/models.py:Player`; запись в `combat._reward()`,
`explore.chest()`, `gathering`, `party`, `factions`, `death`. Экран в
`engine/progress.py`.

**Первая итерация.** Писать только «победа/уровень/добыча» и показывать
последние 10 строк по команде **📜 Журнал**.

## 1.4 👕 Сборки снаряжения (loadouts) + сравнение с надетым

**Сейчас.** Экипировка помнит конкретные экземпляры
(`engine/slots.py`, `p.worn`, `p.equipped_pos`), но **сохранить набор**
нельзя. Для смены «фарм-образ → PvP/босс/мирное» надо по одной кнопке
снимать/надевать 5–6 вещей. Сравнения с надетым тоже нет: на карточке
предмета видно только его статы (`engine/itemui.py`).

**Идея.**

- **Сохранённые образы:** 3 слота сборок «⚔️/🛡/🌿»; внутри — список
  `(slot, uid)`; кнопка «Надеть образ» снимает лишнее и надевает то, что
  есть в сумке/кармане/доме.
- **Сравнение:** на карточке вещи — строка вида
  `Меч+2: +12 АТК (надето: меч+1 → +9 АТК, -3)`. Жёлтым/зелёным выделять,
  где хуже/лучше. Фундамент есть: `core/stats.py:simulate_gear_loadout`
  уже умеет считать гипотетическую экипировку.

**Что даёт.** Возвращает смысл коллекционированию (будущий PvP, боссы с
фазами, фарм на разных классах) без изменения боевых правил.

**Куда.** `engine/slots.py` + `engine/inventory.py` / `engine/progress.py`;
`core/stats.py` для «симуляции образа»; кнопки на карточке предмета.

**Первая итерация.** Одно поле `p.loadout: {"slot": uid}` + команда
«Дать себе 2 образа» для теста; разбор удобств сделать после.

## 1.5 🔕 Настройки уведомлений / тихие часы / дайджест

> ✅ **Реализовано (2026-09-06)** — «Центр вестей» в обоих стеках:
> `engine/notify.py` (правила), `engine/notify_ui.py`, `bot/handlers/notify.py`.
> Каналы аукцион/портал/дуэль + тихие часы, фильтрация в `bot/reminders.py`.
> Прогон: `tests/test_engine_notify.py`, `test_parity.py` (54/54). Дайджест
> (копилка сообщений) сознательно отложен: честнее выключить шум.

**Сейчас.** Есть рассылки и напоминания (`bot/reminders.py`,
`bot/broadcast.py`): о портале, лоте, вызове на дуэль. Но **настроек
«что мне присылать» нет вообще** — сам `IDEAS-next.md` это признаёт в
пункте 10. Админ строит один канал коммуникации для всех.

**Идея.** **Центр вестей** в профиле:

- тумблеры: бои/события/босс/портал/аукцион/отряд/карма;
- «тихие часы» (например 23:00–08:00): за это время копить в один дайджест;
- формат: сразу (default) или раз в N часов;
- отключить всё = полная тишина, но мир продолжает жить.

**Что даёт.** Уважение к игроку (анти-спам) и больше шансов, что весть
про портал не утонет в реакции на чужой лот.

**Куда.** `engine/models.py` -> `p.prefs: dict`; записывать при
`bot/reminders.py` и при публикации из `core/realtime.py`; настройки —
экран `engine/progress.py`/`engine/game.py`.

**Первая итерация.** `p.prefs = {"events": true, "boss": true, "quiet_until": "08"}`;
фильтровать только личные напоминания о порталах и лотах.

## 1.6 🌍 «Хроника мира» — игроку, не только админу

**Сейчас.** Радар событий (`core/realtime.get_radar_feed`, лента на
дашборде админки) — инструмент **владельца**. Игрок про катаклизм/босса/
аукцион узнаёт только из точечных уведомлений или случайно.

**Идея.** **Хроника мира** в боте — короткая живая лента последних событий
сервера с подписью «какие события сейчас активны»:

- ⚠️ «В Тёмном Лесу катаклизм (осталось 2 ч)», вход в описание;
- 🏰 «Пожиратель Глубин — вклад 60 %, до закрытия 4 ч»;
- 🌙 «Растущая луна: +10 % к добыче»;
- 🔥 «Осада цитадели Стражи — роль: защитник, ворота 70 %».

**Что даёт.** Ощущение **живого мира**, а не «личного симулятора». Это те же
данные, что уже есть в `engine/cataclysm`, `engine/worldboss`,
`engine/siege`, `engine/lunar` — не хватает только единого экрана.

**Куда.** Новый `engine/chronicle.py` (собирает из существующих модулей) +
кнопка **📜 Хроника** в меню, формат страницы как у `progress.legends_screen`.

**Первая итерация.** Вывод текущих `cataclysm.active()`, `worldboss`,
`siege.screen()` и `lunar.phase()` одним текстом.

## 1.7 📱 Игровой Telegram Mini App (визуальный клиент)

**Сейчас.** Mini App (`admin/tgapp.py`, `webapp/`) уже есть — но это
**админка**. Игроку по-прежнему только кнопки Telegram: текст + инлайн.

**Идея.** Отдельная **Мини-игра/клиент для игрока** на том же принципе
(HTTPS, подпись `initData`), где:

- настоящая визуальная карта мира/локации с тайлами и туманом;
- драг-инвентарь, сетка экипировки, drag-and-drop в слоты;
- таймеры порталов/боссов/осад с countdown;
- кнопки боя и «быстрые образы» из 1.4;
- вход по кнопке `web_app` прямо из меню бота.

**Что даёт.** Снимает главное ограничение Telegram-интерфейса (нет версии
«в одно сообщение»), а бот остаётся каналом уведомлений. Это самый большой
пункт документа, но у проекта уже есть готовый бэкенд и HTTPS-инфраструктура
(`core/tunnel.py`, `admin/tgapp.py`).

**Куда.** Новый каталог `player/` рядом с `webapp/` (можно переиспользовать
`engine/` как есть), страницы `/play`, `/map`, `/bag`, `/combat`.

**Первая итерация.** Открывать существующий `engine/mapview` как базовую
страницу мини-аппа «Карта» и не трогать Telegram-логику ещё месяц.

---

# Часть 2. Игровые механики

## 2.1 🌦 Погода и время суток

**Сейчас.** Есть фазы луны (`engine/lunar.py`) и «часы мира» в углу админки,
но **ни погоды, ни суток игра не использует**:
`grep -rniE "weather|погода|night|ночь|день|время суток" engine core` — пусто.
Луна меняет популяцию и добычу, а остальной мир статичен.

**Идея.** Глобальный **цикл суток + погода** на таймере (без отдельных
событий-катаклизмов):

- **Сутки:** день/ночь меняют видимость (туман войны), поведение тварей
  (`behavior.hunter` ночью активнее), шанс сундука и засады;
- **Погода:** дождь/туман/буря/снег влияют на рыбалку, травничество,
  раскопки, шанс скрытности и урон стихийных школ;
- **Вести:** при смене погоды — сообщение «Над Трактом сгущается туман:
  +20 % к шансу засады в лесу».

**Что даёт.** Дёшево увеличивает количество решений «куда пойти и когда» —
даже без контента. Ядро уже есть: `settings` + `cataclysm.effects`,
`gathering`, `behavior`, `mapview`.

**Куда.** `engine/weather.py` (правила, сиды как у `cataclysm_seeds`),
встраивание в `do_go`/`do_fight`/`gathering`/`behavior`; шапка мира —
в `texts.cell_view()`.

**Первая итерация.** `p.mood = "day"/"night"` + два множителя у тварей и
засады. Погоду не трогать, пока не найдётся время на визуал.

## 2.2 🔩 Прочность и ремонт снаряжения

> ✅ **Реализовано (2026-09-06)** в обоих стеках. Общие правила в
> `engine/durability.py` (сервер реэкспортирует через `core/durability.py`).
> Материал ремонта — «Ржавый лом» / «Железный лом» + бронза. Прогон:
> `python3 tests/test_engine_durability.py`, `test_parity.py` (55/55),
> `test_wiring.py`.

**Сейчас.** У персонажей **нет износа**:
`grep -rniE "durab|износ|прочност|repair|ремонт" engine core bot` находит
только прочность стен осады (`engine/siege.py`) и хелперы починки замков
(`.repair_corner_castles`). Про крафт/заточку упоминается износ в
`IDEAS-world.md #6` как идея, но не реализован.

**Идея.** **Прочность** у снаряжения (не у всех слоёв — например, оружие и
щит первые), которая падает в бою и при смерти:

- `0–100`; за свой удар `-1`, за полученный удар `-1`, за смерть `-7`;
- множитель цены ремонта растёт с редкостью (×1…×7);
- на 0 вещь «заклинивает»: статы не работают, надеть нельзя;
- **кузнец** в `engine/craft.py` и `bot/handlers/craft.py` получает новый
  вход «🔩 Починить»;
- ремонт стоит **1 Ржавый лом** + бронзу; VIP-скидка сознательно отложена.

**Что даёт.** Долгожданный **сток экономики**: добыча из боя уходит в ремонт,
а не только в коллекцию; заточка получает риск, а не «сделал и забыл».

**Куда.** Новое поле у экземпляра `durability`/`durability_max` есть в
`core/models.py:ItemInstance` и в `engine/items.py:create`; износ зовётся в
`engine/combat.py`, `engine/death.py`, `bot/handlers/battle.py`,
`core/stats.py` (сломанное не усиливает), карточка — в `itemui` /
`bot/utils/texts.py`.

**Первая итерация.** Сделана чуть шире исходного обещания: прочность у всех
5 типов носимой экипировки (не только оружие), ремонт — один вход в
кузнице. Таймеры/ежедневная авто-починка и VIP-скидка не входят.

## 2.3 ⚔️ Боевые приёмы: активные умения с кулдауном

**Сейчас.** В бою (`engine/combat.py`) есть ровно **одно универсальное**
«✨ Умение» за ману. В серверном стеке есть стойки и каст (`bot/handlers/battle.py`),
но это не набор приёмов: выбирать/открывать умения нельзя, кулдаунов нет.
`grep -rniE "cooldown.*skill|способност|ability" engine core` — пусто.

**Идея.** **Боевой репертуар** — 3 активных приёма на класс/подкласс, с
длительностью и кулдауном, сохраняются в профиле:

- Воин: «Сокрушение» (урон ×2, кул 3 раунда), «Щитовой удар» (урон + стана),
  «Клич» (след. атака +15 %);
- Маг: «Ледяная игла» (урон + шанс обездвижить), «пепел» (DoT 3 раунда);
- Разбойник: «Тень» (+уклонение на раунд), «Отравленный клинок»;
- Карма/фракция добавляют свои (Свет — лечебный импульс, Тьма — лайфстил).

**Что даёт.** Бой из «жми Атака/Умение/Блок» превращается в принятие решений
«что применить сейчас, что оставить на фазу босса». Больших изменений в
таблице статов не нужно.

**Куда.** Новый `engine/abilities.py` (каталог + правила),
`engine/combat.py:view/action`, `engine/progress.py` (экран «Приёмы»),
`engine/data.py` (кнопки в бою).

**Первая итерация.** 2 приёма на 3 класса, один кулдаун в секундах, без DoT
и стана — проверить читаемость кнопок.

## 2.4 🧪 Алхимические баффы: расходники «на время», не только «сразу»

**Сейчас.** Расходники есть, но **мгновенные**: `engine/inventory.py:use`
просто лечит HP/MP (`it["bonus"] = {"heal": …}` / `{"mana": …}`).
Временных эффектов (урон, защита, удача для фарма, быстрая починка, аффинити
занятий) нет нигде: `grep -rniE "buff_duration|duration.*bonus|ждёт.*минут" engine core` — пусто.

**Идея.** **Алхимический ряд** поверх уже собранных трав/материалов:

- ⚗️ **Настой силы** — +X% урона на 10 минут;
- 🛡 **Настой стали** — -X% полученного урона на 10 минут;
- 🍀 **Настой удачи** — +X к удаче / +шанс редкости дропа на 30 минут;
- 🌙 **Настой охотника** — +X% к занятиям (рыбалка/травы/раскопки) на
  10 минут;
- ⚠️ однодневное «не стакается», статус видно в «Пульсе» (1.1).

**Что даёт.** Делает работу занятий и сбор крафтовых материалов осмысленной:
у игрока появляется выбор «потратить на XP-буст или на успежный рейд».

**Куда.** `engine/items.py` (новый `bonus={"buff": ...}`),
`engine/inventory.py:use`, `engine/rules.py` (множители), карточка в
`itemui`; таблицы рецептов в `engine/data.py`.

**Первая итерация.** Только 2 зелья (урон → 5 мин; дроп → 10 мин), эффекты
показывать в профиле. Не делать сложности с таймерами на сервере сразу.

## 2.5 🏠 Витрина трофеев в доме (курируемые коллекции)

**Сейчас.** Дом (`engine/homestead.py`) — это сейф и отдых: уровни,
сундук, второй этаж VIP. Есть **достижения** (`engine/titles.py`,
`engine/bestiary.py`) и **летопись предметов**, но выставить трофей на
показ нельзя.

**Идея.** **Витрина** в доме:

- 3–6 «пьедесталов», на них вешаются **именные экземпляры** (из
  `core/items`/`engine/items.py`) или уникальные трофеи (голова босса,
  редкость с первой победы);
- каждая витрина даёт **малый пассивный бонус**: демонстрация реликвии
  «Клинок Пожирателя» -> +2 % к урону по боссам; полный сет «Зимний
  охотник» -> +1 % к редкости дропа;
- посетители (партнёры, гильдия) могут видеть витрину чужого дома при
  нахождении на его клетке.

**Что даёт.** Цель после «полный музей»: у игрока появляется **личный
музей мира** и повод не разбирать уникальные вещи на осколки.

**Куда.** `engine/homestead.py` (+поля `p.display_items`), `engine/items.py`
(проверка редкости/уникальности), `engine/social.py` / `explore.talk()`
(просмотр чужой витрины), `engine/progress.py` (экран «Дом → Витрина»).

**Первая итерация.** Три слота, бонус только от редкости (`common/rare/epic`),
без сетов. Посетителей — не в первый релиз.

## 2.6 🧭 Дозорные поручения: «отправить героя» на таймер

**Сейчас.** В проекте **нет планировщика/фоновых задач**
(`grep -rniE "scheduler" . — пусто`). Игрок может либо играть руками, либо
ждать. VIP-защита «вне мира» есть, но она ничего не делает за игрока.

**Идея.** **Дозорное поручение** — отправка героя на **ограниченное по
времени** задание от NPC/из локации:

- «Осмотреть Тёмный Лес» (10 мин, 40–80🟤, шанс травы);
- «Зачистить тропу» (30 мин, 60–120🟤, шанс материала/лута, **риск
  потерять немного HP**);
- «Сходить за картой» (60 мин, зависит от уровня);
- **ограничения:** 1 активное поручение, нельзя в бою/в данже/на осаде;
- завершается при следующем входе (даты в `p`), а не реальным фоновым
  таймером.

**Что даёт.** Дёшево даёт «идл-прогресс» без pay-to-win: это фарм с выбором
риска, а не автоматический лут. При этом система честно ложится на текущую
модель: у `Player` есть `offline_protected`, `wounded_until`, `quest_day` —
это уже «отложенные» поля.

**Куда.** `engine/gathering.py`/`engine/progress.py` (результат),
`engine/game.py` (кнопка **🚀 Дозор**), `engine/models.py`
(`p.patrol = {"kind": ..., "until": ...}`), `bot/reminders.py`
(пусть «вернулся» — напомнить).

**Первая итерация.** Одно поручение «Зачистить тропу», результат — «статы
+ золото/материал», реализуется только при попытке игрока открыть меню
(без фоновых воркеров).

---

# Часть 3. Что взять первым

По соотношению **впечатление / объём** я бы шёл так:

| Приоритет | Идея | Почему |
|---|---|---|
| 1 | 🩸 Пульс героя (1.1) | Ключевой экран, почти всё есть в данных, меняет ощущение управления |
| 2 | 🔕 Настройки уведомлений (1.5) | Маленькое, закрывает заявленную дыру из `IDEAS-next.md #10` |
| 3 | 🔩 Прочность и ремонт (2.2) | Даёт крафту/экономике мгновенный смысл, минимальная первая итерация |
| 4 | 🧭 Навигатор к цели (1.2) | Прямо снимает боль «не могу найти могилу/тайник/NPC» |
| 5 | ⚔️ Боевые приёмы (2.3) | Самый заметный геймплейный сдвиг после «Пульса» |
| 6 | 🌦 Погода и сутки (2.1) | Дёшево оживляет мир, но лучше после навигатора |
| 7 | 📜 Журнал героя (1.3) | Нарратив и анти-шум; хорош как следующий слой после «Пульса» |
| 8 | 👕 Образы снаряжения (1.4) | Станет очень важным, когда появится PvP/боссы с фазами |
| 9 | 🧪 Алхимические баффы (2.4) | Логичное продолжение крафта, но зависит от 1.1/1.3 для отображения |
| 10 | 🌍 Хроника мира (1.6) | Просто переупаковка существующих данных, можно в любой момент |

Большие пункты — 1.7 (игровой Mini App) и 2.6 (дозорные поручения) — стоит
брать **после** того, как основные экраны привычны: они требуют либо нового
фронта, либо большего количества отложенных состояний.

---

# Что уже есть и чего избегаем

Чтобы не предлагать существующее повторно:

- респавн, мобы-поведение, задания, партии, могилы, карта-туман, боссы,
  фракции, контент вширь — **есть** (`IDEAS-world.md` закрыт);
- порталы/подземелья, подарки, дуэли, осады, престиж/таланты/подклассы,
  сезоны (частично), занятия, дом, сюжет (частично), уведомления —
  **есть/частично**, `IDEAS-next.md`;
- кармы/знамения/луна/руны/фамильяры/арена/гильдии/ломбард/вклады/
  чёрный рынок/реактивные NPC/летопись — **есть** в `README.md` и
  `IDEAS-100.md`;
- археология, карты сокровищ, крафт и заточка, аукцион, торговец — **есть**.

Ниже конкретно **нет** (проверено grep-ом):

- поиск пути / компас к цели;
- сохранённые образы экипировки + сравнение статов;
- настройки уведомлений игрока;
- игровая витрина дома / коллекции;
- погода и полноценный цикл суток;
- наборы активных боевых приёмов с кулдаунами;
- временные алхимические баффы (не мгновенные зелья);
- дозорные поручения (отложенный результата без планировщика);
- игровой Telegram Mini App (сейчас Mini App — админка);

Если какой-то пункт покажется чужеродным для мира — его легко выкинуть
отдельно, не трогая связку остальных.
